<%- include("./includes/head.ejs", { data2: '' }); %>

<link rel="stylesheet" href="/style.css">
<link rel="stylesheet" href="/payment.css">

<body class="bg-light">
  <style>
    @media (max-width: 2500px) {
      #payImg1 {
        width: 300px;
        height: 300px;
      }
    }
    @media (max-width: 500px) {
      #pay1 {
        margin-left: 12px;
      }
      #payImg1 {
        width: 200px;
        height: 200px;
      }
    }
  </style>

  <header class="header">
      <div class="header-inner">
        <div class="container-fluid">
          <nav class="navbar bg-light fixed-top">
            <div class="container-fluid mb-1 justify-content-center display-6">
              <b style="background: black;
                -webkit-background-clip: text;
                -webkit-text-fill-color: transparent;
                font-family: 'Onest'"
              >
                QR
              </b>
            </div>
          </nav>
        </div>
      </div>
    </header>

    <div class="loading-spinner" id="loadingSpinner">
	    <img src="/cube.webp" alt="Your Logo" class="logoSp">
	    <div class="spinner"></div>
   </div>

  <section>
    <div class="container p-2" id="pay1">
      <div class="row justify-content-center" style="margin-top: 70px;">
        <div class="col-12 col-md-3"></div>
        <div class="col-12 col-md-6 border p-3" style="border-radius: 20px;">
          <h5>Pay with <span class="text-uppercase"><%= currency %></span></h5>
          <p>To complete this payment send exactly this amount to the address provided.</p>
          <div class="bg-secondary d-flex justify-content-between align-items-center mt-2 mb-3 p-3" style="border-radius: 5px;">
            <div class="text-uppercase"><%= currency %></div>
            <div><%= amount %></div>
          </div>
          <div class="d-flex justify-content-between align-items-center mb-3 p-3" style=" border-radius: 5px;">
            <div>Amount</div>
            <div><%= cAmt %> <span class="text-uppercase">usd</span></div>
          </div>
          <div class="d-flex flex-column justify-content-center align-items-center mb-3">
            <img id="payImg1" />
            <p class="mt-3">Only send <span class="text-uppercase"><%= currency %></span> to this address</p>
          </div>
          <div class="border p-1 bg-light mb-3" style="border-radius: 25px; color: black;">
            <div class="row justify-content-center align-items-center">
              <div class="col-10 border-0 text-center">
                <div class="text-break" style="overflow: hidden;"><%= address %></div>
              </div>
              <div class="col-2 p-0 border-0 text-end" style="border-left: 2px solid black;">
                <button class="copy-button border-0">Copy</button>
              </div>
            </div>
          </div>
          <div class="mb-3">
            <a href="#" class="btn btn-outline-dark text-uppercase p-2 w-100 text-center" style="text-decoration: none; color: black; border-radius: 20px;">Continue</a>
          </div>
        </div>
        <div class="col-12 col-md-3"></div>
      </div>
    </div>
  </section>

  <section>
    <div class="container" style="height: 100px;"></div>
  </section>

  <footer>
    <div class="container">
      <div class="row">
        <div class="col" style="background: white;">
          <nav class="navbar fixed-bottom bg-light border">
            <div class="container div6">
              <a class="navbar-brand" href="/">
                <img src="https://api.hiphopboombox.com/api/uploads/hiphop2.jpeg" alt="" height="40" />
              </a>

              <div classs="div9">
                <a href="/privacy" class="footer_a">
                  <%= lang.footer_h2 %>
                </a>
                <a href="/terms" class="footer_a">
                  <%= lang.footer_h3 %>
                </a>
                <a href="/dmca" class="footer_a">
                  <%= lang.footer_h4 %>
                </a>
                <a href="/eula" class="footer_a">
                  <%= lang.footer_h5 %>
                </a>
                <a href="/raffle" class="footer_a">
                  <%= lang.footer_h6 %>
                </a>
               </div>

              <div></div>
            </div>
          </nav>
        </div>
      </div>
    </div>
  </footer>

  <script>
    window.addEventListener('load', function () {
        // Get the loading spinner element
        const loadingSpinner = document.getElementById('loadingSpinner');
          
        // Hide the loading spinner
        loadingSpinner.style.display = 'none';
      });
  </script>

  <script>
    const iSRC = '<%- JSON.stringify(iSrc) %>';
  </script>

  <script type="text/javascript">
    globalThis.document.addEventListener('DOMContentLoaded', () => {
      const payImg2 = JSON.parse(iSRC);

      const payImg1 = globalThis.document.getElementById("payImg1");

      payImg1.setAttribute("src", payImg2);

      // console.log(payImg1, payImg2);

      const copyButtons = document.querySelectorAll('.copy-button');

      copyButtons.forEach(button => {
        button.addEventListener('click', async () => {
            const textToCopy = button.parentElement.previousElementSibling.firstElementChild.innerText;

            try {
              await navigator.clipboard.writeText(textToCopy);
              alert('Address copied to clipboard!');
            } catch (err) {
              console.error('Failed to copy:', err);
            }
        });
      });

      function changeIconColor(element) {
        // Remove 'activeNav' class from all links
        sdocument.querySelectorAll('.nav-link i').forEach(icon => {
          icon.style.color = '#ffffff';
        });

        document.querySelectorAll('.nav-link span').forEach(text => {
          text.style.color = 'white';
        });

        // Set the clicked icon and text to green
        let icon = link.querySelector('i');
        icon.style.color = '#7CFC00';

        let text = link.querySelector('span');
        text.style.color = '#7CFC00';
      }
    })
  </script>

<%- include("./includes/end.ejs"); %>